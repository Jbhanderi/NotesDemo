<div class="row">

    <div class="col-lg-6" id="divNotes">
        @{ Html.RenderAction("GetNotes","Note"); }
    </div>

    <div class="col-lg-6">

        <div class="row margin-top-10">
            <div class="col-lg-12">
                <button type="button" class="btn btn-default pull-right" id="btnAddNewNote">Add Notes</button>
            </div>
        </div>

        <div class="row">
            <div class="col-lg-12">
                <div id="divNote">

                </div>
            </div>

            @*@{ Html.RenderPartial("_Note", Model); }*@
        </div>

    </div>
</div>

@section scripts{

    <script type="text/javascript">
        var urlHome_GetNotes = '@Url.Action("GetNotes")';
        var urlHome_GetNotePartialView = '@Url.Action("GetNotePartialView")';
        var urlHome_AddOrUpdateNote = '@Url.Action("AddOrUpdateNote")';
        var urlHome_DeleteNote = '@Url.Action("DeleteNote")';

    </script>

    @Scripts.Render("~/bundles/Note")
}
